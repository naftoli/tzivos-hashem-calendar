node_modules/
build/
# the built single-file app is what the Resources calendar tab serves,
# so it ships; everything else the build drops does not
dist/*
coverage/
.DS_Store
*.log
.env*
!.env.example
.cursor/
# Windows download markers that ride along with the source files
*:Zone.Identifier
