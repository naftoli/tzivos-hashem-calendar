node_modules/
build/
# the built single-file app is what the Resources calendar tab links to,
# so it ships; everything else the build drops does not
dist/*
!dist/index.html
coverage/
.DS_Store
*.log
.env*
!.env.example
.cursor/
# Windows download markers that came along with the source files
*:Zone.Identifier
